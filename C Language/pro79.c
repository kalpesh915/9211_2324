#include<stdio.h>

void main(){
    int x = 15; /// 15 is constant (decimal)

    printf("\n value of x is %d", x);
    x = 010; /// octal
    printf("\n value of x is %o", x);
    x = 0x17; /// hex decimal
    printf("\n value of x is %x", x);
}
