#include<stdio.h>

/**
    pointer is used to get address of any variable or to point on
    given address
*/

void main(){
    int ip1 = 100;

    printf("\n value of ip1 is %d", ip1);
    printf("\n Address of ip1 is %d", &ip1);
    printf("\n Address of ip1 is %x", &ip1);
}
